
<tr  class="table-active">
	<td><%= format_date(review.created_at) %></td>
	<td><%= review.autor%></td>
  <td><%= review.descripcion%></td>
  <td><%= review.rev%></td>
  <td><%= link_to "Borrar", poke_review_path(review.poke, review), method: :delete, data: {confirm: "Estas Seguro?"} %></td>
</tr>
